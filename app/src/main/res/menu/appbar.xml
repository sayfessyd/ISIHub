<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/server_item"
        android:icon="@drawable/ic_cloud2"
        android:title="@string/cloud_label"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/account_item"
        android:icon="@drawable/ic_account2"
        android:title="@string/account_label"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/help_item"
        android:icon="@drawable/ic_help"
        android:title="@string/help_label"
        app:showAsAction="never"/>
    <item android:id="@+id/settings_item"
        android:icon="@drawable/ic_settings"
        android:title="@string/settings_label"
        app:showAsAction="never"/>
</menu>